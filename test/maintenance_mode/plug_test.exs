defmodule MaintenanceMode.PlugTest do
  use ExUnit.Case

  alias Plug.Conn

  describe "init/1" do
    # TODO
  end

  describe "call/2" do
    test "send 200 when maintenance mode disabled"

    test "send 503 when maintenance mode enabled"
  end
end
